# TODO - Ajuste de Login/Rotas (Spring Security)

- [x] Revisar rotas do React para garantir que a tela de autenticação `/login` apareça primeiro
- [x] Atualizar `web/src/App.jsx` para usar `AuthProvider` e configurar rotas `/login` e `/tasks` com `ProtectedRoute`
- [ ] Testar: iniciar back + front sem token deve renderizar `/login`; com token deve renderizar `/tasks`


